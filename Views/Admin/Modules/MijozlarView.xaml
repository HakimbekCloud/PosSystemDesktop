<UserControl x:Class="PosSystem.Views.Admin.Modules.MijozlarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:MijozlarViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0" Title="Mijozlar" Sub="Sodiqlik darajalari, qarzlar va xarid tarixi">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButton}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE898;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Import"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi mijoz"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,6,0" Label="JAMI MIJOZLAR"    Value="{Binding KpiTotal}"  Glyph="&#xE716;" Tone="Brand"/>
                    <c:StatCard Margin="3,0,3,0" Label="YANGI MIJOZLAR"   Value="{Binding KpiNew}"    Glyph="&#xE77B;" Tone="Success"/>
                    <c:StatCard Margin="3,0,3,0" Label="FAOL MIJOZLAR"    Value="{Binding KpiActive}" Unit="ta" Glyph="&#xE9D9;" Tone="Info"/>
                    <c:StatCard Margin="6,0,0,0" Label="UMUMIY QARZDORLIK" Value="{Binding KpiDebt}"  Glyph="&#xE7BB;" Tone="Warning"/>
                </UniformGrid>

                <Border Style="{StaticResource DataGridShell}" Margin="0,16,0,0">
                    <DataGrid Style="{StaticResource AdminDataGrid}" ItemsSource="{Binding Customers}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="MIJOZ" Width="*" MinWidth="260">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="34" Height="34" CornerRadius="999" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource Avatar0Bg}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Background" Value="{StaticResource Avatar1Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Background" Value="{StaticResource Avatar2Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Background" Value="{StaticResource Avatar3Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Background" Value="{StaticResource Avatar4Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Background" Value="{StaticResource Avatar5Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Background" Value="{StaticResource Avatar6Bg}"/></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Initials}" FontWeight="Bold" FontSize="12"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource Avatar0Fg}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Foreground" Value="{StaticResource Avatar1Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Foreground" Value="{StaticResource Avatar2Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Foreground" Value="{StaticResource Avatar3Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Foreground" Value="{StaticResource Avatar4Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Foreground" Value="{StaticResource Avatar5Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Foreground" Value="{StaticResource Avatar6Fg}"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource N900}"/>
                                                <TextBlock Text="{Binding Phone}" FontFamily="{StaticResource UiFontMono}"
                                                           FontSize="11" Foreground="{StaticResource N500}" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="DARAJA" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <c:StatusBadge Text="{Binding Tier}">
                                            <c:StatusBadge.Style>
                                                <Style TargetType="c:StatusBadge">
                                                    <Setter Property="Tone" Value="Neutral"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Oltin"><Setter Property="Tone" Value="Warning"/></DataTrigger>
                                                        <DataTrigger Binding="{Binding Tier}" Value="Kumush"><Setter Property="Tone" Value="Info"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </c:StatusBadge.Style>
                                        </c:StatusBadge>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="TASHRIFLAR" Binding="{Binding Visits}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="OXIRGI TASHRIF" Binding="{Binding LastVisit}" Width="130"/>
                            <DataGridTextColumn Header="UMUMIY XARID" Binding="{Binding TotalFmt}" Width="170">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="QARZ" Binding="{Binding DebtFmt}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasDebt}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource Danger600}"/>
                                                <Setter Property="FontWeight"  Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE890;"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE70F;"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE715;"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
