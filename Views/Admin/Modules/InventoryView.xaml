<UserControl x:Class="PosSystem.Views.Admin.Modules.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:InventoryViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource SurfaceApp}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0"
                      Crumb="ShefPos / Ombor"
                      Title="Ombor boshqaruvi"
                      Subtitle="Kirim, chiqim va inventarizatsiya · joriy qoldiq 184 220 000 so'm">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButtonSm}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Hisobot"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminSecondaryButtonSm}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE899;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Inventarizatsiya"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminPrimaryButtonSm}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi kirim"/>
                            <Border Style="{StaticResource KbdHint}" Background="#FFFFFF" Margin="8,0,0,0">
                                <TextBlock Text="N" Style="{StaticResource KbdHintText}"/>
                            </Border>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,16,20,24">

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,8,0" Label="OMBOR QIYMATI" Value="{Binding KpiValue}"    Delta="{Binding KpiValueDelta}" DeltaTone="Up"   Sub="joriy hisobotda" Glyph="&#xE7B8;" Tone="Brand"/>
                    <c:StatCard Margin="4,0,4,0" Label="KIRIM"        Value="{Binding KpiInbound}"   Sub="{Binding KpiInboundSub}"  Glyph="&#xE898;" Tone="Success"/>
                    <c:StatCard Margin="4,0,4,0" Label="CHIQIM"       Value="{Binding KpiOutbound}"  Sub="{Binding KpiOutboundSub}" Glyph="&#xE74C;" Tone="Info"/>
                    <c:StatCard Margin="8,0,0,0" Label="OGOHLANTIRISHLAR" Value="{Binding KpiAlerts}" Sub="{Binding KpiAlertsSub}" Glyph="&#xE7BA;" Tone="Danger"/>
                </UniformGrid>

                <!-- Tabs -->
                <TabControl Style="{StaticResource AdminTabControl}" Margin="0,16,0,0">

                    <!-- ── Movements ── -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Harakatlar">
                        <Border Background="{StaticResource Surface1}"
                                BorderBrush="{StaticResource BorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <DockPanel>
                                <Border DockPanel.Dock="Top" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1" Padding="12,10">
                                    <StackPanel Orientation="Horizontal">
                                        <Grid Width="280" Margin="0,0,8,0">
                                            <TextBox x:Name="MovementsSearchBox" Style="{StaticResource AdminTextBoxSm}" Padding="30,0,8,0"/>
                                            <TextBlock Text="&#xE721;" FontFamily="{StaticResource FluentIcons}" FontSize="12"
                                                       Foreground="{StaticResource N400}" VerticalAlignment="Center"
                                                       Margin="10,0,0,0" IsHitTestVisible="False"/>
                                            <TextBlock Text="Hujjat raqami yoki kontragent…"
                                                       Style="{StaticResource HintText}" VerticalAlignment="Center"
                                                       Margin="32,0,0,0" IsHitTestVisible="False"
                                                       Visibility="{Binding ElementName=MovementsSearchBox, Path=Text, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
                                        </Grid>
                                        <ComboBox Style="{StaticResource AdminComboBoxSm}" Width="150" Margin="0,0,8,0">
                                            <ComboBoxItem IsSelected="True">Turi: Barchasi</ComboBoxItem>
                                            <ComboBoxItem>Kirim</ComboBoxItem>
                                            <ComboBoxItem>Chiqim</ComboBoxItem>
                                            <ComboBoxItem>Inventarizatsiya</ComboBoxItem>
                                            <ComboBoxItem>Yaroqsiz</ComboBoxItem>
                                        </ComboBox>
                                        <ComboBox Style="{StaticResource AdminComboBoxSm}" Width="160">
                                            <ComboBoxItem IsSelected="True">Davr: Bu hafta</ComboBoxItem>
                                            <ComboBoxItem>Bu oy</ComboBoxItem>
                                            <ComboBoxItem>Bu yil</ComboBoxItem>
                                        </ComboBox>
                                    </StackPanel>
                                </Border>

                                <DataGrid Style="{StaticResource AdminDataGrid}"
                                          ItemsSource="{Binding Movements}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="HUJJAT №" Binding="{Binding Number}"  Width="120" FontFamily="{StaticResource UiFontMono}"/>
                                        <DataGridTemplateColumn Header="TURI" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <c:StatusBadge>
                                                        <c:StatusBadge.Style>
                                                            <Style TargetType="c:StatusBadge">
                                                                <Setter Property="Tone" Value="Neutral"/>
                                                                <Setter Property="Text" Value="{Binding Type}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Type}" Value="Kirim"><Setter Property="Tone" Value="Success"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Type}" Value="Chiqim"><Setter Property="Tone" Value="Info"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Type}" Value="Inventarizatsiya"><Setter Property="Tone" Value="Brand"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Type}" Value="Yaroqsiz"><Setter Property="Tone" Value="Danger"/></DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </c:StatusBadge.Style>
                                                    </c:StatusBadge>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="SANA"    Binding="{Binding Date}"    Width="150"/>
                                        <DataGridTextColumn Header="KONTRAGENT" Binding="{Binding Partner}" Width="*"/>
                                        <DataGridTextColumn Header="TOVAR"   Binding="{Binding Items}"   Width="80"/>
                                        <DataGridTextColumn Header="SUMMA"   Binding="{Binding TotalFormatted}" Width="160">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                    <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Header="HOLAT" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <c:StatusBadge>
                                                        <c:StatusBadge.Style>
                                                            <Style TargetType="c:StatusBadge">
                                                                <Setter Property="Tone" Value="Success"/>
                                                                <Setter Property="Text" Value="Tasdiqlangan"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Status}" Value="pending">
                                                                        <Setter Property="Tone" Value="Warning"/>
                                                                        <Setter Property="Text" Value="Tasdiqlash kerak"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </c:StatusBadge.Style>
                                                    </c:StatusBadge>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Header="" Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <Button Style="{StaticResource RowActionButton}" Content="&#xE890;"/>
                                                        <Button Style="{StaticResource RowActionButton}" Content="&#xE749;"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>
                        </Border>
                    </TabItem>

                    <!-- ── Current stock ── -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Joriy qoldiq">
                        <Border Background="{StaticResource Surface1}"
                                BorderBrush="{StaticResource BorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <DataGrid Style="{StaticResource AdminDataGrid}"
                                      ItemsSource="{Binding StockLines}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="KOD"        Binding="{Binding Code}"     Width="90" FontFamily="{StaticResource UiFontMono}"/>
                                    <DataGridTextColumn Header="NOMI"       Binding="{Binding Name}"     Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Foreground" Value="{StaticResource N900}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="KATEGORIYA" Binding="{Binding Category}" Width="180"/>
                                    <DataGridTextColumn Header="QOLDIQ"     Binding="{Binding StockFormatted}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="MINIMUM"    Binding="{Binding Min}"  Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="QIYMAT"     Binding="{Binding ValueFormatted}" Width="160">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="TREND"      Binding="{Binding TrendFormatted}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Foreground" Value="{StaticResource Success700}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTemplateColumn Header="HOLAT" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <c:StatusBadge>
                                                    <c:StatusBadge.Style>
                                                        <Style TargetType="c:StatusBadge">
                                                            <Setter Property="Tone" Value="Success"/>
                                                            <Setter Property="Text" Value="Yetarli"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="low">
                                                                    <Setter Property="Tone" Value="Warning"/>
                                                                    <Setter Property="Text" Value="Kam qoldiq"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="out">
                                                                    <Setter Property="Tone" Value="Danger"/>
                                                                    <Setter Property="Text" Value="Tugagan"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </c:StatusBadge.Style>
                                                </c:StatusBadge>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </TabItem>

                    <!-- ── Alerts ── -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Ogohlantirishlar">
                        <Border Background="{StaticResource Surface1}"
                                BorderBrush="{StaticResource BorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ItemsControl ItemsSource="{Binding Alerts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource N100}" BorderThickness="0,0,0,1" Padding="16,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="32"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Width="28" Height="28" CornerRadius="6"
                                                        VerticalAlignment="Center">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource Warning50}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Severity}" Value="danger">
                                                                    <Setter Property="Background" Value="{StaticResource Danger50}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Severity}" Value="info">
                                                                    <Setter Property="Background" Value="{StaticResource Info50}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="&#xE7BA;" FontFamily="{StaticResource FluentIcons}" FontSize="14"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource Warning600}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="danger">
                                                                        <Setter Property="Foreground" Value="{StaticResource Danger600}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Severity}" Value="info">
                                                                        <Setter Property="Foreground" Value="{StaticResource Info600}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>

                                                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Code}" FontFamily="{StaticResource UiFontMono}" FontSize="11" Foreground="{StaticResource N500}" Margin="0,0,8,0"/>
                                                        <TextBlock Text="{Binding Name}" Style="{StaticResource BodyText}" FontWeight="SemiBold" Foreground="{StaticResource N900}"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Detail}" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding When}" Style="{StaticResource HintText}" Margin="0,0,12,0"/>
                                                    <Button Style="{StaticResource AdminSecondaryButtonSm}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="11" Margin="0,0,6,0"/>
                                                            <TextBlock Text="To'ldirish"/>
                                                        </StackPanel>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>
                    </TabItem>
                </TabControl>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
