<UserControl x:Class="PosSystem.Views.Admin.Modules.OmborView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:OmborViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0" Title="Ombor" Sub="Kirim, chiqim, transfer va inventarizatsiya">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButton}" Content="Inventar" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource AdminSecondaryButton}" Content="Transfer" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi kirim"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,6,0" Label="OMBOR QIYMATI"  Value="{Binding KpiValue}"   Glyph="&#xE7B8;" Tone="Brand"/>
                    <c:StatCard Margin="3,0,3,0" Label="BUGUNGI KIRIM"   Value="{Binding KpiInbound}" Glyph="&#xE898;" Tone="Success" Delta="+12.4%" DeltaTone="Up"/>
                    <c:StatCard Margin="3,0,3,0" Label="BUGUNGI CHIQIM"  Value="{Binding KpiOutbound}" Glyph="&#xE76C;" Tone="Info"/>
                    <c:StatCard Margin="6,0,0,0" Label="DIQQAT TALAB"    Value="{Binding KpiAlerts}" Unit="ta" Glyph="&#xE7BA;" Tone="Warning"/>
                </UniformGrid>

                <TabControl Style="{StaticResource AdminTabControl}" Margin="0,16,0,0">
                    <!-- ─ Operatsiyalar ─ -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Operatsiyalar">
                        <Border Style="{StaticResource DataGridShell}">
                            <DataGrid Style="{StaticResource AdminDataGrid}" ItemsSource="{Binding Movements}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="SANA"        Binding="{Binding Date}"   Width="120"/>
                                    <DataGridTextColumn Header="HUJJAT"      Binding="{Binding Doc}"    Width="120" FontFamily="{StaticResource UiFontMono}"/>
                                    <DataGridTemplateColumn Header="TURI" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <c:StatusBadge>
                                                    <c:StatusBadge.Style>
                                                        <Style TargetType="c:StatusBadge">
                                                            <Setter Property="Tone" Value="Neutral"/>
                                                            <Setter Property="Text" Value="{Binding Type}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Type}" Value="kirim"><Setter Property="Tone" Value="Success"/><Setter Property="Text" Value="Kirim"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="chiqim"><Setter Property="Tone" Value="Info"/><Setter Property="Text" Value="Chiqim"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="transfer"><Setter Property="Tone" Value="Brand"/><Setter Property="Text" Value="Transfer"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="writeoff"><Setter Property="Tone" Value="Danger"/><Setter Property="Text" Value="Yaroqsizlik"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="inventory"><Setter Property="Tone" Value="Warning"/><Setter Property="Text" Value="Inventar"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </c:StatusBadge.Style>
                                                </c:StatusBadge>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="KONTRAGENT" Binding="{Binding Partner}" Width="*"/>
                                    <DataGridTextColumn Header="POZ."        Binding="{Binding Items}"  Width="70"/>
                                    <DataGridTextColumn Header="SUMMA"       Binding="{Binding SumFmt}" Width="140">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="MAS'UL"     Binding="{Binding By}"   Width="140"/>
                                    <DataGridTemplateColumn Header="HOLATI" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <c:StatusBadge>
                                                    <c:StatusBadge.Style>
                                                        <Style TargetType="c:StatusBadge">
                                                            <Setter Property="Tone" Value="Success"/>
                                                            <Setter Property="Text" Value="Tasdiqlangan"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="progress">
                                                                    <Setter Property="Tone" Value="Info"/>
                                                                    <Setter Property="Text" Value="Jarayonda"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </c:StatusBadge.Style>
                                                </c:StatusBadge>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </TabItem>

                    <!-- ─ Joriy qoldiq ─ -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Joriy qoldiq">
                        <Border Style="{StaticResource DataGridShell}">
                            <DataGrid Style="{StaticResource AdminDataGrid}" ItemsSource="{Binding Stock}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="MAHSULOT" Binding="{Binding Name}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Foreground" Value="{StaticResource N900}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="KATEGORIYA" Binding="{Binding Cat}" Width="160"/>
                                    <DataGridTextColumn Header="QOLDIQ"     Binding="{Binding StockFmt}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="MIN" Binding="{Binding Min}" Width="60">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTemplateColumn Header="HOLATI" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <c:StatusBadge>
                                                    <c:StatusBadge.Style>
                                                        <Style TargetType="c:StatusBadge">
                                                            <Setter Property="Tone" Value="Success"/>
                                                            <Setter Property="Text" Value="Yetarli"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="low">
                                                                    <Setter Property="Tone" Value="Warning"/>
                                                                    <Setter Property="Text" Value="Kam qoldiq"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="out">
                                                                    <Setter Property="Tone" Value="Danger"/>
                                                                    <Setter Property="Text" Value="Tugagan"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </c:StatusBadge.Style>
                                                </c:StatusBadge>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="TANNARX (JAMI)" Binding="{Binding ValueFmt}" Width="160">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </TabItem>

                    <!-- ─ Ogohlantirishlar ─ -->
                    <TabItem Style="{StaticResource AdminTabItem}" Header="Ogohlantirishlar">
                        <ItemsControl ItemsSource="{Binding Alerts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource AdminCard}" Padding="16" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource Warning50}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Severity}" Value="danger">
                                                                <Setter Property="Background" Value="{StaticResource Danger50}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="&#xE7BA;" FontFamily="{StaticResource FluentIcons}" FontSize="22"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource Warning600}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Severity}" Value="danger">
                                                                    <Setter Property="Foreground" Value="{StaticResource Danger600}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource N900}"/>
                                                <TextBlock Text="{Binding Detail}" Style="{StaticResource MutedText}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <c:StatusBadge Grid.Column="2" Text="{Binding Right}" Margin="12,0">
                                                <c:StatusBadge.Style>
                                                    <Style TargetType="c:StatusBadge">
                                                        <Setter Property="Tone" Value="Warning"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Severity}" Value="danger">
                                                                <Setter Property="Tone" Value="Danger"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </c:StatusBadge.Style>
                                            </c:StatusBadge>
                                            <Button Grid.Column="3" Style="{StaticResource AdminSecondaryButtonSm}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                                                    <TextBlock Text="Buyurtma"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
