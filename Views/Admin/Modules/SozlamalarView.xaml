<UserControl x:Class="PosSystem.Views.Admin.Modules.SozlamalarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:SozlamalarViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">

    <UserControl.Resources>
        <Style x:Key="SettingsTab" TargetType="ListBoxItem">
            <Setter Property="FontFamily"        Value="{StaticResource UiFont}"/>
            <Setter Property="FontSize"          Value="14"/>
            <Setter Property="Foreground"        Value="{StaticResource N700}"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Padding"           Value="0"/>
            <Setter Property="Margin"            Value="0,1,0,1"/>
            <Setter Property="FocusVisualStyle"  Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root" Background="Transparent" Padding="12,0" Height="36" CornerRadius="6">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                              TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource N100}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource Brand50}"/>
                                <Setter Property="Foreground" Value="{StaticResource Brand700}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0" Title="Sozlamalar" Sub="Tizim, qurilmalar, integratsiyalar va xavfsizlik">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminGhostButton}" Content="Bekor qilish" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="O'zgarishlarni saqlash"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="24,16,24,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: tab list -->
                <Border Grid.Column="0"
                        Background="{StaticResource Surface1}"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="6"
                        VerticalAlignment="Top">
                    <ListBox Background="Transparent" BorderThickness="0"
                             ItemsSource="{Binding Tabs}"
                             SelectedItem="{Binding SelectedTab}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource SettingsTab}"/>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource FluentIconText}" Text="{Binding Glyph}" FontSize="14"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Title}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- Right: form sections -->
                <StackPanel Grid.Column="2">
                    <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                    <!-- Tashkilot ma'lumotlari -->
                    <Border Style="{StaticResource AdminCard}" Margin="0,0,0,12" Padding="0">
                        <StackPanel>
                            <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1" Padding="18,14">
                                <StackPanel>
                                    <TextBlock Text="Tashkilot ma'lumotlari" Style="{StaticResource CardTitleText}"/>
                                    <TextBlock Text="Chek va hisobotlarda ko'rinadigan rekvizitlar" Style="{StaticResource CardSubText}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                            <StackPanel Margin="18,14">
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Tashkilot nomi" Style="{StaticResource BodyText}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Style="{StaticResource AdminTextBox}" Text="{Binding BusinessName, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                                <Border Height="1" Background="{StaticResource N100}" Margin="0,12"/>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Yuridik manzil" Style="{StaticResource BodyText}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Style="{StaticResource AdminTextBox}" Text="{Binding LegalAddress, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                                <Border Height="1" Background="{StaticResource N100}" Margin="0,12"/>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="14"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="INN / Telefon" Style="{StaticResource BodyText}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBox  Grid.Column="1" Style="{StaticResource AdminTextBox}" Text="{Binding Inn, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBox  Grid.Column="3" Style="{StaticResource AdminTextBox}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Toggles section -->
                    <Border Style="{StaticResource AdminCard}" Padding="0">
                        <StackPanel>
                            <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1" Padding="18,14">
                                <StackPanel>
                                    <TextBlock Text="Funksiyalar" Style="{StaticResource CardTitleText}"/>
                                    <TextBlock Text="Mahalliylashtirish va xulq-atvor sozlamalari" Style="{StaticResource CardSubText}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                            <StackPanel Margin="18,8,18,14">
                                <Grid Margin="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Sodiqlik tizimi" Style="{StaticResource BodyText}" FontWeight="SemiBold"/>
                                        <TextBlock Text="Mijozlarga ball va chegirma berish" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" Style="{StaticResource AdminToggle}" IsChecked="{Binding EnableLoyalty}"/>
                                </Grid>
                                <Border Height="1" Background="{StaticResource N100}" Margin="0,4"/>
                                <Grid Margin="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Onlayn to'lovlar" Style="{StaticResource BodyText}" FontWeight="SemiBold"/>
                                        <TextBlock Text="Click, Payme, karta bilan to'lash" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" Style="{StaticResource AdminToggle}" IsChecked="{Binding EnableOnlinePayments}"/>
                                </Grid>
                                <Border Height="1" Background="{StaticResource N100}" Margin="0,4"/>
                                <Grid Margin="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Avtomatik chek bosib chiqarish" Style="{StaticResource BodyText}" FontWeight="SemiBold"/>
                                        <TextBlock Text="Sotuv yakunida fiskal chek avtomatik chiqadi" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" Style="{StaticResource AdminToggle}" IsChecked="{Binding AutoPrintReceipt}"/>
                                </Grid>
                                <Border Height="1" Background="{StaticResource N100}" Margin="0,4"/>
                                <Grid Margin="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Avtomatik chiqish" Style="{StaticResource BodyText}" FontWeight="SemiBold"/>
                                        <TextBlock Text="20 daqiqa harakatsizlikdan keyin" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" Style="{StaticResource AdminToggle}" IsChecked="{Binding AutoLogout}"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
