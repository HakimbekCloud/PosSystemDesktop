<UserControl x:Class="PosSystem.Views.Admin.Modules.MahsulotlarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:MahsulotlarViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0"
                      Title="Mahsulotlar"
                      Sub="Katalogda 15 ta nomenklatura · 6 ta kategoriya">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButton}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE898;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Import"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminSecondaryButton}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Eksport"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi mahsulot"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <!-- KPI strip -->
                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,6,0" Label="JAMI MAHSULOTLAR" Value="{Binding KpiTotal}" Unit="ta" Glyph="&#xE7B8;" Tone="Brand"/>
                    <c:StatCard Margin="3,0,3,0" Label="OMBOR QIYMATI"    Value="{Binding KpiValue}"          Glyph="&#xE7E8;" Tone="Info"/>
                    <c:StatCard Margin="3,0,3,0" Label="KAM QOLDIQ"       Value="{Binding KpiLow}"   Unit="ta" Glyph="&#xE7BA;" Tone="Warning"/>
                    <c:StatCard Margin="6,0,0,0" Label="TUGAGAN"          Value="{Binding KpiOut}"   Unit="ta" Glyph="&#xE711;" Tone="Danger"/>
                </UniformGrid>

                <!-- Toolbar -->
                <Border Background="{StaticResource Surface1}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1"
                        CornerRadius="8" Padding="14,10" Margin="0,16,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Grid Width="320" Margin="0,0,8,0">
                                <TextBox x:Name="SearchBox" Style="{StaticResource AdminTextBox}"
                                         Padding="34,0,12,0"
                                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="&#xE721;" FontFamily="{StaticResource FluentIcons}" FontSize="13"
                                           Foreground="{StaticResource N400}" VerticalAlignment="Center"
                                           Margin="12,0,0,0" IsHitTestVisible="False"/>
                                <TextBlock Text="Nomi, kodi yoki barkod bo'yicha qidirish..."
                                           Style="{StaticResource HintText}" VerticalAlignment="Center"
                                           Margin="36,0,0,0" IsHitTestVisible="False"
                                           Visibility="{Binding ElementName=SearchBox, Path=Text, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
                            </Grid>
                            <ComboBox Style="{StaticResource AdminComboBox}" Width="180" Margin="0,0,8,0">
                                <ComboBoxItem IsSelected="True">Barcha filiallar</ComboBoxItem>
                                <ComboBoxItem>Chilonzor</ComboBoxItem>
                                <ComboBoxItem>Mirobod</ComboBoxItem>
                                <ComboBoxItem>Markaziy ombor</ComboBoxItem>
                            </ComboBox>
                            <ComboBox Style="{StaticResource AdminComboBox}" Width="180">
                                <ComboBoxItem IsSelected="True">Statusi: Barchasi</ComboBoxItem>
                                <ComboBoxItem>Faol</ComboBoxItem>
                                <ComboBoxItem>Kam qoldiq</ComboBoxItem>
                                <ComboBoxItem>Tugagan</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Category chips -->
                <ItemsControl ItemsSource="{Binding Categories}" Margin="0,0,0,12">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Style="{StaticResource AdminChip}" Margin="0,0,6,6">
                                <TextBlock Text="{Binding}"/>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Table -->
                <Border Style="{StaticResource DataGridShell}">
                    <DataGrid Style="{StaticResource AdminDataGrid}" ItemsSource="{Binding Products}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="KOD"        Binding="{Binding Code}" Width="90" FontFamily="{StaticResource UiFontMono}"/>
                            <DataGridTextColumn Header="MAHSULOT"   Binding="{Binding Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource N900}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="KATEGORIYA" Binding="{Binding Category}" Width="180"/>
                            <DataGridTextColumn Header="SOTISH NARXI" Binding="{Binding PriceFmt}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="TANNARX"    Binding="{Binding CostFmt}"  Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="QOLDIQ"     Binding="{Binding StockFmt}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="HOLATI" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <c:StatusBadge>
                                            <c:StatusBadge.Style>
                                                <Style TargetType="c:StatusBadge">
                                                    <Setter Property="Tone" Value="Success"/>
                                                    <Setter Property="Text" Value="Faol"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="low">
                                                            <Setter Property="Tone" Value="Warning"/>
                                                            <Setter Property="Text" Value="Kam qoldiq"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="out">
                                                            <Setter Property="Tone" Value="Danger"/>
                                                            <Setter Property="Text" Value="Tugagan"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </c:StatusBadge.Style>
                                        </c:StatusBadge>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE890;" ToolTip="Ko'rish"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE70F;" ToolTip="Tahrirlash"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE749;" ToolTip="Yorliq"/>
                                            <Button Style="{StaticResource RowActionDangerButton}" Content="&#xE74D;" ToolTip="O'chirish"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
