<UserControl x:Class="PosSystem.Views.Admin.Modules.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:CustomersViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource SurfaceApp}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0"
                      Crumb="ShefPos / Mijozlar"
                      Title="Mijozlar bazasi"
                      Subtitle="Sodiqlik darajalari, qarzlar va xarid tarixi">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButtonSm}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE898;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Import"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminSecondaryButtonSm}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE715;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="SMS yuborish"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource AdminPrimaryButtonSm}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi mijoz"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,16,20,24">

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,8,0" Label="JAMI MIJOZLAR"     Value="{Binding KpiTotal}"  Unit="{Binding KpiTotalUnit}"  Sub="{Binding KpiTotalSub}"  Glyph="&#xE716;" Tone="Brand"/>
                    <c:StatCard Margin="4,0,4,0" Label="YANGI MIJOZLAR"    Value="{Binding KpiNew}"    Sub="{Binding KpiNewSub}"    Glyph="&#xE77B;" Tone="Success"/>
                    <c:StatCard Margin="4,0,4,0" Label="UMUMIY QARZDORLIK" Value="{Binding KpiDebt}"   Sub="{Binding KpiDebtSub}"   Glyph="&#xE7BB;" Tone="Warning"/>
                    <c:StatCard Margin="8,0,0,0" Label="FAOL MIJOZLAR"     Value="{Binding KpiActive}" Unit="{Binding KpiActiveUnit}" Sub="{Binding KpiActiveSub}" Glyph="&#xE9D9;" Tone="Info"/>
                </UniformGrid>

                <!-- Toolbar -->
                <Border Background="{StaticResource Surface1}"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="1"
                        CornerRadius="8,8,0,0"
                        Padding="12,10"
                        Margin="0,16,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Grid Width="320" Margin="0,0,8,0">
                                <TextBox x:Name="CustomersSearch" Style="{StaticResource AdminTextBoxSm}" Padding="30,0,8,0"/>
                                <TextBlock Text="&#xE721;" FontFamily="{StaticResource FluentIcons}" FontSize="12"
                                           Foreground="{StaticResource N400}" VerticalAlignment="Center"
                                           Margin="10,0,0,0" IsHitTestVisible="False"/>
                                <TextBlock Text="Ism, telefon yoki ID bo'yicha…"
                                           Style="{StaticResource HintText}" VerticalAlignment="Center"
                                           Margin="32,0,0,0" IsHitTestVisible="False"
                                           Visibility="{Binding ElementName=CustomersSearch, Path=Text, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
                            </Grid>
                            <ComboBox Style="{StaticResource AdminComboBoxSm}" Width="170" Margin="0,0,8,0">
                                <ComboBoxItem IsSelected="True">Daraja: Barchasi</ComboBoxItem>
                                <ComboBoxItem>Oltin</ComboBoxItem>
                                <ComboBoxItem>Kumush</ComboBoxItem>
                                <ComboBoxItem>Bronza</ComboBoxItem>
                            </ComboBox>
                            <ComboBox Style="{StaticResource AdminComboBoxSm}" Width="170">
                                <ComboBoxItem IsSelected="True">Qarz: Barchasi</ComboBoxItem>
                                <ComboBoxItem>Qarzdor</ComboBoxItem>
                                <ComboBoxItem>Qarzsiz</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                        <Border Grid.Column="1" Style="{StaticResource SegmentedGroupBorder}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Style="{StaticResource SegmentedItem}" GroupName="CustView" IsChecked="True" Content="Jadval"/>
                                <RadioButton Style="{StaticResource SegmentedItem}" GroupName="CustView" Content="Kartochka"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Border Background="{StaticResource Surface1}"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="1,0,1,1"
                        CornerRadius="0,0,8,8">
                    <DockPanel>
                        <Border DockPanel.Dock="Bottom" Style="{StaticResource DataGridFooter}">
                            <TextBlock Style="{StaticResource HintText}">
                                <Run Text="Jami:"/>
                                <Run Text="{Binding Customers.Count, Mode=OneWay}" FontWeight="SemiBold"/>
                                <Run Text="mijoz · 1 dan 15 gacha"/>
                            </TextBlock>
                        </Border>

                        <DataGrid Style="{StaticResource AdminDataGrid}"
                                  ItemsSource="{Binding Customers}">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="MIJOZ" Width="*" MinWidth="240">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Border Width="32" Height="32" CornerRadius="999"
                                                        VerticalAlignment="Center">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource Avatar0Bg}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Background" Value="{StaticResource Avatar1Bg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Background" Value="{StaticResource Avatar2Bg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Background" Value="{StaticResource Avatar3Bg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Background" Value="{StaticResource Avatar4Bg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Background" Value="{StaticResource Avatar5Bg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Background" Value="{StaticResource Avatar6Bg}"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Initials}"
                                                               FontWeight="Bold" FontSize="11"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource Avatar0Fg}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Foreground" Value="{StaticResource Avatar1Fg}"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Foreground" Value="{StaticResource Avatar2Fg}"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Foreground" Value="{StaticResource Avatar3Fg}"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Foreground" Value="{StaticResource Avatar4Fg}"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Foreground" Value="{StaticResource Avatar5Fg}"/></DataTrigger>
                                                                    <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Foreground" Value="{StaticResource Avatar6Fg}"/></DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource N900}" FontSize="13"/>
                                                    <TextBlock Text="{Binding Phone}" Style="{StaticResource HintText}" FontFamily="{StaticResource UiFontMono}" Margin="0,1,0,0"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="DARAJA" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <c:StatusBadge Text="{Binding Tier}">
                                                <c:StatusBadge.Style>
                                                    <Style TargetType="c:StatusBadge">
                                                        <Setter Property="Tone" Value="Neutral"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Oltin"><Setter Property="Tone" Value="Warning"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Kumush"><Setter Property="Tone" Value="Info"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Tier}" Value="Bronza"><Setter Property="Tone" Value="Neutral"/></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </c:StatusBadge.Style>
                                            </c:StatusBadge>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="TASHRIFLAR" Binding="{Binding Visits}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="OXIRGI TASHRIF" Binding="{Binding LastVisit}" Width="130"/>
                                <DataGridTextColumn Header="UMUMIY XARID" Binding="{Binding TotalFormatted}" Width="160">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="QARZ" Binding="{Binding DebtFormatted}" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasDebt}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource Danger600}"/>
                                                    <Setter Property="FontWeight"  Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="" Width="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button Style="{StaticResource RowActionButton}" Content="&#xE890;" ToolTip="Profil"/>
                                                <Button Style="{StaticResource RowActionButton}" Content="&#xE715;" ToolTip="SMS"/>
                                                <Button Style="{StaticResource RowActionButton}" Content="&#xE70F;" ToolTip="Tahrirlash"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
