<UserControl x:Class="PosSystem.Views.Admin.Modules.SotuvView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:SotuvViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0"
                      Title="Sotuv"
                      Sub="Kassa terminali — bu bo'lim alohida tayyorlangan (CashierShell)"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <!-- Kassa link card -->
                <Border Style="{StaticResource AdminCard}" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="64" Height="64"
                                Background="{StaticResource Brand50}"
                                CornerRadius="14" VerticalAlignment="Center">
                            <TextBlock Text="&#xE7BF;" FontFamily="{StaticResource FluentIcons}"
                                       FontSize="28" Foreground="{StaticResource Brand600}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Kassir paneli" FontSize="16" FontWeight="Bold" Foreground="{StaticResource N900}"/>
                            <TextBlock Style="{StaticResource MutedText}" Margin="0,4,0,0"
                                       Text="Mahsulot tanlash, savat, to'lov va chek chiqarish loyihaning kassir bo'limida tayyor."/>
                        </StackPanel>
                        <Button Grid.Column="2" Style="{StaticResource AdminPrimaryButton}" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Kassani ochish" VerticalAlignment="Center"/>
                                <TextBlock Text="&#xE76C;" FontFamily="{StaticResource FluentIcons}"
                                           FontSize="13" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

                <!-- KPI strip -->
                <UniformGrid Columns="4" Rows="1" Margin="0,16,0,0">
                    <c:StatCard Margin="0,0,6,0" Label="BUGUNGI SAVDO" Value="{Binding KpiRevenue}" Glyph="&#xE825;" Tone="Brand"   Delta="+12.4% kechagidan" DeltaTone="Up"/>
                    <c:StatCard Margin="3,0,3,0" Label="CHEKLAR SONI"  Value="{Binding KpiOrders}"  Unit="ta" Glyph="&#xE7C1;" Tone="Info"  Delta="+8 chek"           DeltaTone="Up"/>
                    <c:StatCard Margin="3,0,3,0" Label="O'RTACHA CHEK" Value="{Binding KpiAverage}" Glyph="&#xE9D9;" Tone="Success" Delta="+3.1%"             DeltaTone="Up"/>
                    <c:StatCard Margin="6,0,0,0" Label="QAYTARISHLAR"  Value="{Binding KpiReturns}" Unit="ta" Glyph="&#xE895;" Tone="Warning" Delta="-2 ta"              DeltaTone="Down"/>
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
