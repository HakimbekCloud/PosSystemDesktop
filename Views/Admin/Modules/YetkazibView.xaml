<UserControl x:Class="PosSystem.Views.Admin.Modules.YetkazibView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:YetkazibViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0" Title="Yetkazib beruvchilar" Sub="Kontragentlar, qarzdorlik va buyurtmalar">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButton}" Content="Eksport" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi yetkazib beruvchi"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,6,0" Label="JAMI"             Value="{Binding KpiTotal}"  Unit="ta" Glyph="&#xE7C1;" Tone="Brand"/>
                    <c:StatCard Margin="3,0,3,0" Label="FAOL"             Value="{Binding KpiActive}" Unit="ta" Glyph="&#xE73E;" Tone="Success"/>
                    <c:StatCard Margin="3,0,3,0" Label="UMUMIY QARZDORLIK" Value="{Binding KpiDebt}"           Glyph="&#xE7BA;" Tone="Warning"/>
                    <c:StatCard Margin="6,0,0,0" Label="KUTILAYOTGAN"     Value="{Binding KpiOrders}" Unit="ta" Glyph="&#xE710;" Tone="Info"/>
                </UniformGrid>

                <Border Style="{StaticResource DataGridShell}" Margin="0,16,0,0">
                    <DataGrid Style="{StaticResource AdminDataGrid}" ItemsSource="{Binding Suppliers}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="KOD" Binding="{Binding Code}" Width="100" FontFamily="{StaticResource UiFontMono}"/>
                            <DataGridTextColumn Header="NOMI" Binding="{Binding Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource N900}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="KONTAKT"    Binding="{Binding Contact}"  Width="180" FontFamily="{StaticResource UiFontMono}"/>
                            <DataGridTextColumn Header="KATEGORIYA" Binding="{Binding Category}" Width="180"/>
                            <DataGridTextColumn Header="BUYURTMA"   Binding="{Binding Orders}"   Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="QARZ" Binding="{Binding DebtFmt}" Width="160">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="{StaticResource UiFontMono}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="{StaticResource N500}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasDebt}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource Danger600}"/>
                                                <Setter Property="FontWeight"  Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="OXIRGI BUYURTMA" Binding="{Binding LastOrder}" Width="140"/>
                            <DataGridTemplateColumn Header="HOLATI" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <c:StatusBadge>
                                            <c:StatusBadge.Style>
                                                <Style TargetType="c:StatusBadge">
                                                    <Setter Property="Tone" Value="Success"/>
                                                    <Setter Property="Text" Value="Faol"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="overdue">
                                                            <Setter Property="Tone" Value="Danger"/>
                                                            <Setter Property="Text" Value="Kechikkan"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="inactive">
                                                            <Setter Property="Tone" Value="Neutral"/>
                                                            <Setter Property="Text" Value="Nofaol"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </c:StatusBadge.Style>
                                        </c:StatusBadge>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE890;"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE710;" ToolTip="Yangi buyurtma"/>
                                            <Button Style="{StaticResource RowActionButton}" Content="&#xE70F;"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
