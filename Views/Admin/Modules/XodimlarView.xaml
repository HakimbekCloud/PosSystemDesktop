<UserControl x:Class="PosSystem.Views.Admin.Modules.XodimlarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PosSystem.ViewModels.Admin.Modules"
             xmlns:c="clr-namespace:PosSystem.Views.Admin.Controls"
             d:DataContext="{d:DesignInstance Type=vm:XodimlarViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource AppSurface}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <c:PageHeader Grid.Row="0" Title="Xodimlar" Sub="Kassirlar, menejerlar, omborchilar va ruxsatlar boshqaruvi">
            <c:PageHeader.Actions>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource AdminSecondaryButton}" Content="Smena jadvali" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource AdminPrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="{StaticResource FluentIcons}" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Yangi xodim"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </c:PageHeader.Actions>
        </c:PageHeader>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,16,24,24">
                <c:WpfMappingBanner Hint="{Binding WpfHint}"/>

                <UniformGrid Columns="4" Rows="1">
                    <c:StatCard Margin="0,0,6,0" Label="JAMI"         Value="{Binding KpiTotal}"   Unit="xodim" Glyph="&#xE716;" Tone="Brand"/>
                    <c:StatCard Margin="3,0,3,0" Label="SMENADA"      Value="{Binding KpiOnShift}" Unit="ta"    Glyph="&#xE77B;" Tone="Success"/>
                    <c:StatCard Margin="3,0,3,0" Label="OYLIK FONDI"  Value="{Binding KpiPayroll}"              Glyph="&#xE7BF;" Tone="Info"/>
                    <c:StatCard Margin="6,0,0,0" Label="DAVOMAT"      Value="{Binding KpiRate}"                 Glyph="&#xE73E;" Tone="Warning"/>
                </UniformGrid>

                <!-- Employee cards (WrapPanel) -->
                <ItemsControl ItemsSource="{Binding Employees}" Margin="0,16,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource AdminCard}" Padding="18" Width="240" Margin="0,0,12,12">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0" Width="52" Height="52">
                                            <Border Width="52" Height="52" CornerRadius="999">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource Avatar0Bg}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Background" Value="{StaticResource Avatar1Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Background" Value="{StaticResource Avatar2Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Background" Value="{StaticResource Avatar3Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Background" Value="{StaticResource Avatar4Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Background" Value="{StaticResource Avatar5Bg}"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Background" Value="{StaticResource Avatar6Bg}"/></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Initials}" FontWeight="Bold" FontSize="16"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource Avatar0Fg}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Accent}" Value="1"><Setter Property="Foreground" Value="{StaticResource Avatar1Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="2"><Setter Property="Foreground" Value="{StaticResource Avatar2Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="3"><Setter Property="Foreground" Value="{StaticResource Avatar3Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="4"><Setter Property="Foreground" Value="{StaticResource Avatar4Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="5"><Setter Property="Foreground" Value="{StaticResource Avatar5Fg}"/></DataTrigger>
                                                                <DataTrigger Binding="{Binding Accent}" Value="6"><Setter Property="Foreground" Value="{StaticResource Avatar6Fg}"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <!-- Online indicator dot -->
                                            <Ellipse Width="14" Height="14"
                                                     HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                     Margin="0,0,-2,-2"
                                                     Stroke="White" StrokeThickness="2">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="{StaticResource Success500}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="offline">
                                                                <Setter Property="Fill" Value="{StaticResource N400}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="leave">
                                                                <Setter Property="Fill" Value="{StaticResource Warning500}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </Grid>
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource N900}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Role}" Style="{StaticResource HintText}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                    <Border Background="{StaticResource SurfaceMuted}" CornerRadius="6" Padding="10" Margin="0,12,0,0">
                                        <StackPanel>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Filial" Style="{StaticResource HintText}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Branch}" Style="{StaticResource BodyText}" FontSize="12" FontWeight="Medium"/>
                                            </Grid>
                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Smena" Style="{StaticResource HintText}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Shift}" Style="{StaticResource BodyText}" FontSize="12" FontWeight="Medium"/>
                                            </Grid>
                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Telefon" Style="{StaticResource HintText}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Phone}" FontFamily="{StaticResource UiFontMono}" FontSize="12" Foreground="{StaticResource N700}"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
