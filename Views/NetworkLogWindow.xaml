<Window x:Class="PosSystem.Views.NetworkLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PosSystem.Services"
        Title="Network Monitor" Height="700" Width="1100"
        Background="#1E1E2E" Foreground="#CDD6F4"
        FontFamily="Consolas" FontSize="12"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <!-- Status badge color -->
        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsError}" Value="True">
                    <Setter Property="Foreground" Value="#F38BA8"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsError}" Value="False">
                    <Setter Property="Foreground" Value="#A6E3A1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MethodText" TargetType="TextBlock">
            <Setter Property="Width" Value="44"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Method}" Value="POST">
                    <Setter Property="Foreground" Value="#FAB387"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Method}" Value="GET">
                    <Setter Property="Foreground" Value="#89DCEB"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Method}" Value="PUT">
                    <Setter Property="Foreground" Value="#F9E2AF"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Method}" Value="DELETE">
                    <Setter Property="Foreground" Value="#F38BA8"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ListRowStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,3"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#313244"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BodyBox" TargetType="TextBox">
            <Setter Property="Background"       Value="#11111B"/>
            <Setter Property="Foreground"       Value="#CDD6F4"/>
            <Setter Property="BorderBrush"      Value="#45475A"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8"/>
            <Setter Property="IsReadOnly"       Value="True"/>
            <Setter Property="TextWrapping"     Value="Wrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="AcceptsReturn"    Value="True"/>
        </Style>

        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#BAC2DE"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ClearBtn" TargetType="Button">
            <Setter Property="Background"   Value="#313244"/>
            <Setter Property="Foreground"   Value="#CDD6F4"/>
            <Setter Property="BorderBrush"  Value="#45475A"/>
            <Setter Property="Padding"      Value="12,4"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- toolbar -->
            <RowDefinition Height="*"/>      <!-- list -->
            <RowDefinition Height="5"/>      <!-- splitter -->
            <RowDefinition Height="260"/>    <!-- detail -->
        </Grid.RowDefinitions>

        <!-- ── Toolbar ── -->
        <Border Grid.Row="0" Background="#181825" Padding="10,6">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="Network Monitor"
                           FontSize="13" FontWeight="Bold" Foreground="#CBA6F7"
                           VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="CountLabel" VerticalAlignment="Center"
                               Foreground="#6C7086" Margin="0,0,12,0"/>
                    <Button Content="Tozalash" Style="{StaticResource ClearBtn}" Click="OnClearClick"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ── Request list ── -->
        <ListView x:Name="LogList" Grid.Row="1"
                  Background="#1E1E2E" BorderThickness="0"
                  ItemContainerStyle="{StaticResource ListRowStyle}"
                  SelectionChanged="OnSelectionChanged"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Vaqt"    Width="90">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TimeLabel}" Foreground="#6C7086"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Metod"   Width="54">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Method}" Style="{StaticResource MethodText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Status"  Width="54">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StatusLabel}" Style="{StaticResource StatusText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Davomiylik" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DurationLabel}" Foreground="#BAC2DE" TextAlignment="Right" Width="70"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="URL" Width="750">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ShortUrl}" Foreground="#CDD6F4"
                                           TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- ── Splitter ── -->
        <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch"
                      Background="#313244" Height="5" Cursor="SizeNS"/>

        <!-- ── Detail panel ── -->
        <Grid Grid.Row="3" Background="#181825">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Request body -->
            <Grid Grid.Column="0" Margin="8,6,4,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="So'rov (Request Body)"
                           Style="{StaticResource HeaderLabel}"/>
                <TextBox Grid.Row="1" x:Name="ReqBodyBox"
                         Style="{StaticResource BodyBox}"
                         FontSize="11"/>
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                          Background="#313244" Cursor="SizeWE"/>

            <!-- Response body -->
            <Grid Grid.Column="2" Margin="4,6,8,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Javob (Response Body)"
                           Style="{StaticResource HeaderLabel}"/>
                <TextBox Grid.Row="1" x:Name="RespBodyBox"
                         Style="{StaticResource BodyBox}"
                         FontSize="11"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
